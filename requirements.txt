# Pinned to the exact versions installed in the environment that produced the standing
# GW153-183 baseline (2086) - TODO 4.2: an unpinned GBM bump (CatBoost especially) can
# silently move realized-points numbers with no code change. To upgrade deliberately:
# bump the pin, re-run the standard backtest, and compare vs the baseline with
# `python -m fpl.milp.compare_backtests` before adopting. Each run's actual library
# versions are also stamped into experiments/results.csv by fpl/experiment.py.
pandas==3.0.3
numpy==2.4.6
pulp==3.3.2
highspy==1.15.1  # HiGHS MILP solver backend (config.MILP_SOLVER="highs"); CBC remains the fallback
lightgbm==4.6.0
xgboost==3.2.0
catboost==1.2.10
scikit-learn==1.9.0
scipy==1.17.1
requests==2.34.2
pytest==9.1.1
statsmodels==0.14.6  # only for the per-player ARIMA baseline (fpl.model.baselines, imported lazily)
optuna==4.9.0  # only needed for fpl.model.tuning (imported lazily there)

# Notebook-only (notebooks/eda.ipynb); not used by the pipeline or tests.
matplotlib==3.11.0
seaborn==0.13.2
ipykernel==7.3.0
