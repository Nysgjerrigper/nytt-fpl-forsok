pandas>=2.0
numpy>=1.24
pulp>=2.7
highspy>=1.7  # HiGHS MILP solver backend (config.MILP_SOLVER="highs"); CBC remains the fallback
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
scikit-learn>=1.3
scipy>=1.10
requests>=2.31
pytest>=7
statsmodels>=0.14  # only for the per-player ARIMA baseline (fpl.model.baselines, imported lazily)
optuna>=4.0  # only needed for fpl.model.tuning (imported lazily there)

# Notebook-only (notebooks/eda.ipynb); not used by the pipeline or tests.
matplotlib>=3.8
seaborn>=0.13
ipykernel>=6.29
